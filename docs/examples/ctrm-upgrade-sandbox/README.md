# CTRM 升级场景（脱敏沙盒）

本目录用**虚构/脱敏**的列表页片段，模拟工作中的：

- 约数十个列表页迁移到 `gd-search-form-config` / `BaseForm v-model="queryForm"`
- 窄布局 `form-actions-col` 防挤叠
- 边界页：分页字段与 `condition` 拆分

用于在个人仓库开 **真实 PR**，验证 PR Intent Checker，**不包含公司机密代码**。
